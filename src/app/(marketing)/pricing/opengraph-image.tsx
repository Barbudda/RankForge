// File-convention OG image for /pricing — reuses the root card so social
// shares of the pricing page get a real preview (twitter:card is
// summary_large_image site-wide).
export { default, size, contentType, alt } from "../../opengraph-image";
